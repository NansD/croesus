{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "expense-service/api/expense.js",
  "scripts": {
    "start": "cd expense-service && sls offline start --httpPort 3001",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "cd expense-service && sls deploy",
    "lint": "eslint --format codeframe .",
    "prettier-check": "prettier -c --ignore-path .gitignore \"**/*.{css,html,js,json,md,yaml,yml}\"",
    "prettify": "prettier --write --ignore-path .gitignore \"**/*.{css,html,js,json,md,yaml,yml}\"",
    "postinstall": "cd expense-service && npm i"
  },
  "keywords": [],
  "author": "NansD",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.2.0",
    "eslint-config-serverless": "0.0.5",
    "eslint-plugin-import": "^2.21.2",
    "eslint-plugin-node": "^11.1.0",
    "git-list-updated": "^1.2.1",
    "prettier": "^2.0.5"
  },
  "dependencies": {}
}
